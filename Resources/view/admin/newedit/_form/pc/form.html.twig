{% trans_default_domain('article.admin') %}

<div>
    {{ form_start(form) }}

    {{ form_row(form.title, { label : 'Заголовок статьи'|trans, placeholder : 'Укажите заголовок' } ) }}

    {{ form_row(form.content, { label : 'Содержимое статьи'|trans, placeholder : 'Задайте содержимое статьи' } ) }}

{#    {{ form_row(form.type, { label : 'Тип профиля'|trans, placeholder : 'Выберите тип профиля из списка...' } ) }}#}

    {{ form_end(form) }}
</div>

